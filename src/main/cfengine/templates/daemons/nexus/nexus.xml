<?xml version="1.0" encoding="UTF-8"?>
<nexusConfiguration>
  <version>2.7.0</version>
  <nexusVersion>{{nexusVersion}}</nexusVersion>
  <globalConnectionSettings>
    <connectionTimeout>20000</connectionTimeout>
    <retrievalRetryCount>3</retrievalRetryCount>
  </globalConnectionSettings>
  <remoteProxySettings />
  <restApi>
    <baseUrl>{{baseUrl}}</baseUrl>
    <uiTimeout>60000</uiTimeout>
  </restApi>
  <httpProxy>
    <enabled>true</enabled>
    <port>8082</port>
    <proxyPolicy>strict</proxyPolicy>
  </httpProxy>
  <routing>
    <resolveLinks>true</resolveLinks>
  </routing>
  <repositories>
    {{#repositories}}
    <repository>
      <id>{{id}}</id>
      <name>{{name}}</name>
      <providerRole>org.sonatype.nexus.proxy.repository.Repository</providerRole>
      <providerHint>{{providerHint}}</providerHint>
      <localStatus>IN_SERVICE</localStatus>
      {{#notFoundCacheActive}}
      <notFoundCacheActive>true</notFoundCacheActive>
      {{/notFoundCacheActive}}
      <notFoundCacheTTL>1440</notFoundCacheTTL>
      <userManaged>true</userManaged>
      <exposed>true</exposed>
      <browseable>true</browseable>
      <writePolicy>{{writePolicy}}</writePolicy>
      {{#indexable}}
      <indexable>true</indexable>
      {{/indexable}}
      <searchable>true</searchable>
      <localStorage>
        <provider>file</provider>
      </localStorage>
      {{#remoteStorage}}
      <remoteStorage>
        <url>{{url}}</url>
      </remoteStorage>
      {{/remoteStorage}}
      <externalConfiguration>
        {{#remoteRepositoryAccess}}
        <artifactMaxAge>{{artifactMaxAge}}</artifactMaxAge>
        <itemMaxAge>1440</itemMaxAge>
        <cleanseRepositoryMetadata>false</cleanseRepositoryMetadata>
        <downloadRemoteIndex>true</downloadRemoteIndex>
        <checksumPolicy>WARN</checksumPolicy>
        {{/remoteRepositoryAccess}}
        <repositoryPolicy>{{repositoryPolicy}}</repositoryPolicy>
      </externalConfiguration>
    </repository>
    {{/repositories}}
  </repositories>
  <repositoryGrouping />
  <repositoryTargets>
    {{#repositoryTargets}}
    <repositoryTarget>
      <id>{{id}}</id>
      <name>{{name}}</name>
      <contentClass>{{contentClass}}</contentClass>
      <patterns>
        {{#patterns}}
        <pattern>{{.}}</pattern>
        {{/patterns}}
      </patterns>
    </repositoryTarget>
    {{/repositoryTargets}}
  </repositoryTargets>
  <smtpConfiguration>
    <hostname>smtp-host</hostname>
    <port>25</port>
    <username>smtp-username</username>
    <password></password>
    <systemEmailAddress>system@nexus.org</systemEmailAddress>
  </smtpConfiguration>
  <notification>
    <notificationTargets>
      <notificationTarget>
        <targetId>autoBlockTarget</targetId>
      </notificationTarget>
    </notificationTargets>
  </notification>
</nexusConfiguration>
